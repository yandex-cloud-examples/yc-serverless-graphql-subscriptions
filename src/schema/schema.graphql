type ConnectionNotFound {
  connectionId: String!
}

type Message {
  from: String!
  text: String!
}

type Mutation {
  sendMessage(connectionId: String!, text: String!): SendMessageResult!
}

type Query {
  me: String!
}

union SendMessageResult = ConnectionNotFound | Message

type Subscription {
  messages: [Message]
}